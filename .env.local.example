/**
 * -----------------------------------------------------------------------------
 * Environment Configuration
 * -----------------------------------------------------------------------------
 * This file documents all required environment variables for local development
 * and production deployments.
 *
 * Copy this file to `.env` and replace placeholder values with real credentials.
 *
 * IMPORTANT:
 * - Never commit real secrets to version control.
 * - `.env` should always be ignored via `.gitignore`.
 * -----------------------------------------------------------------------------
 */

/**
 * -----------------------------------------------------------------------------
 * Convex Deployment Configuration
 * -----------------------------------------------------------------------------
 */

/**
 * Unique Convex deployment identifier.
 *
 * Used by:
 * - `npx convex dev`
 * - Convex backend services
 *
 * Example:
 * CONVEX_DEPLOYMENT=dev:my-project-123
 */
CONVEX_DEPLOYMENT=<your-deployment-id>

/**
 * Public Convex backend URL.
 *
 * Used by:
 * - Frontend (Vite)
 * - Convex client SDK
 *
 * Example:
 * VITE_CONVEX_URL=https://my-project.convex.cloud
 */
VITE_CONVEX_URL=<your-convex-url>


/**
 * -----------------------------------------------------------------------------
 * AI Configuration
 * -----------------------------------------------------------------------------
 */

/**
 * Google Gemini API key.
 *
 * Used for:
 * - AI-powered features (text generation, analysis, etc.)
 *
 * Obtain from:
 * https://ai.google.dev/
 */
GEMINI_API_KEY=<your-gemini-api-key>


/**
 * -----------------------------------------------------------------------------
 * Authentication (Clerk)
 * -----------------------------------------------------------------------------
 */

/**
 * Clerk JWT issuer domain.
 *
 * Used for:
 * - Verifying Clerk-issued JWTs on the backend
 * - Secure authentication and authorization
 *
 * Example:
 * CLERK_JWT_ISSUER_DOMAIN=https://clerk.your-app.com
 */
CLERK_JWT_ISSUER_DOMAIN=<your-clerk-jwt-issuer-domain>

/**
 * -----------------------------------------------------------------------------
 * Clerk Frontend Configuration
 * -----------------------------------------------------------------------------
 */

/**
 * Clerk publishable key.
 *
 * Used by:
 * - Frontend applications (Vite)
 * - Initializing Clerk client SDK
 *
 * Safe to expose in the browser.
 *
 * Example:
 * VITE_CLERK_PUBLISHABLE_KEY=pk_test_abc123
 */
VITE_CLERK_PUBLISHABLE_KEY=pk_test_placeholder

/**
 * -----------------------------------------------------------------------------
 * Clerk Backend Configuration
 * -----------------------------------------------------------------------------
 */

/**
 * Clerk secret key.
 *
 * Used by:
 * - Backend services
 * - Server-side authentication and user management
 *
 * SECURITY:
 * - Must never be exposed to the client
 * - Must never be committed with real values
 *
 * Example:
 * CLERK_SECRET_KEY=sk_test_abc123
 */
CLERK_SECRET_KEY=sk_test_placeholder
